import "./Workers.scoped.css";

import classNames from "classnames";

export default function Workers() {
  return (
    <div className={classNames("Workers")}>
      <p>Workers</p>
    </div>
  );
}
